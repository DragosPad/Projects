@*@using MvcRestaurant.ViewModel*@
@*@using MvcRestaurant.Models*@

@model MvcRestaurant.ViewModel.WaiterTableViewModel
          @using MvcRestaurant.Models

@{
    ViewBag.Title = "TablesView";
}

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>TABLE</legend>
        
    </fieldset>
    <table>
 <tr>
    <th>
      Table ID
     </th>
    <th>
    Dimension
    </th>
     <th>
         Status
     </th>
     <th>
         Chooese status
     </th>

    <th></th>
  </tr>
@foreach (var item in Model.Waiter.Tables)
{ 
    <tr>
        <td>
            @item.TableId
        </td>
        <td> 
            @Html.DisplayFor(modelItem => item.DimensionTable)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <td>
            @Html.ActionLink("Select status and write note", "WriteNote", "Waiters", new { tableId = item.TableId }, null)
   
           @* @if (item.Status == Status.Free)
            {
                 @Html.ActionLink("Select status and write note", "WriteNote", "Waiters", new { tableId = item.TableId }, null)
               
            }
            else
            {
             @Html.ActionLink("Select status and write note", "WriteNote", "Waiters", null, new { tableId = item.TableId, @class = "Not-active" })
            }*@
        </td>
 
    </tr>
 
}
</table>

}
<div>
    @Html.ActionLink("Back to List", "Index")
</div>
